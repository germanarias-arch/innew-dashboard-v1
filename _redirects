# Cloudflare: las Functions en /api/* tienen prioridad; los assets (index.html, login.html) se sirven directo.
# Sin catch-all: Cloudflare Workers rechaza "/* /index.html 200" por loop infinito (2026-07-31).
